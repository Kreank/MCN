"""KI-Fundament (Schema `ai`). Adapter, Executor, Werkzeuge, Workflows.

Leitsatz: Die KI schreibt nie direkt. Sie erzeugt einen `ai_proposal` ohne
fachliche Wirkung; ausgeführt wird der ausschließlich über die Fach-API — durch
dieselben Tore wie beim Menschen. Siehe `docs/ki-orchestrierung.md`.
"""
